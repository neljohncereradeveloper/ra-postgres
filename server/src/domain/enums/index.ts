export enum BallotStatus {
  PENDING = 'pending',
  ISSUED = 'issued',
  CAST = 'cast',
  VOID = 'void',
}

export enum ElectionStatus {
  SCHEDULED = 'scheduled',
  STARTED = 'started',
  CLOSED = 'closed',
  CANCELLED = 'cancelled',
}
